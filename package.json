{
  "name": "lambda-validate-user",
  "version": "0.0.1",
  "description": "Lambda to validate a user from a Cognito User Pool",
  "main": "validate.js",
  "dependencies": {
    "axios": "^0.18.0",
    "jsonwebtoken": "^8.2.1",
    "jwk-to-pem": "^2.0.0"
  },
  "devDependencies": {
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ian Finch",
  "license": "ISC"
}
